# 项目专属记忆与AI开发规范 (Project Memory & AI Rules)

## 1. 弹窗规范 (UI Rules)
- **绝对禁止使用浏览器原生的弹窗**：不要使用 `window.alert`, `window.confirm`, `window.prompt`。
- **强制使用内部弹窗**：所有需要用户确认或提示的信息，都必须使用项目内封装好的 `Modal.tsx` 组件来进行局部/全屏弹窗。
- **债务显示规范**：当在 UI 中展示“债务 (Debt)”相关数值时，不要在前面加负号（例如显示 `💰1000` 而不是 `-💰1000`），因为“债务”二字已经包含了负债的语意。

## 2. 文本规范 (Language)
- 游戏内面向玩家的所有文本必须**全部使用中文**。
- 例如：不能写 "dmg" 而要写 "伤害"，不能写 "hp" 而是写 "耐久"。

## 3. 打包发布流程 (Build & Package)
- 已经在 `package.json` 中配置了成熟的自动化脚本。
- 每次完成功能并更新版本后，**不要手动去敲打包和压缩命令**，直接运行统一脚本：`npm run build:zip`。
- 运行该脚本后会自动进行 TypeScript 编译、Vite 打包，并使用自带的 `tar` 将 `dist` 目录安全压缩为 `ship-happens-dist.zip`（彻底避免 Windows 反斜杠导致的 Linux 部署 404 问题）。
- **必须更新版本号与文档（核心记忆）**：每次完成新功能的修改后，务必主动更新版本号（同步修改 `package.json` 和 `src/games/sink-or-rich/content/version.ts`），并同步更新项目说明文档（`README.md`），确保开发日志和版本进度是最新的。

## 4. 游戏设计理念 (Game Design Core)
- 强调成长与解锁机制。加入诱惑性画大饼元素（比如用 `???` 和 `未解锁` 显示高阶段位内容）。
- 游戏的剧情和功能应通过玩家的财富、声望、通缉值来动态解锁触发，而非一次性全给。
